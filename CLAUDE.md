# 企鵝媽媽部落格 — 發文 SOP

使用者只會提供「主題 + 重點/大綱」與「一個裝照片的資料夾路徑」。收到後照下面流程走完，不用每次都問。

## 1. 收集輸入
- 主題、重點/大綱：使用者直接在對話裡給
- 照片：使用者會告訴一個電腦上的資料夾路徑

## 2. 決定 slug 與分類
- 用英文幫文章取一個 kebab-case 的 slug（例如 `okinawa-with-kids`），檔名和圖片資料夾都用這個
- 分類四選一：`parenting`（企鵝寶寶長大中）/ `working`（大人的功課）/ `travel`（兩個人的行李箱）/ `solo`（住在行李箱的那幾年，當媽媽以前、走過七大洲的視角），根據主題判斷；不確定就選最貼近的，不用特地問

## 3. 處理圖片
```bash
node scripts/process-images.mjs "<使用者給的資料夾路徑>" <slug>
```
- 會自動轉 webp、壓縮、限制寬度 1600px，輸出到 `public/images/<slug>/<slug>-1.webp`、`-2.webp`...
- 寫文章時視內容需要，用 `![替代文字](/images/<slug>/<slug>-1.webp)` 插入到合適段落，不用每張都塞，挑跟內容搭的

## 4. 寫文章
- 新增 `src/content/blog/<slug>.md`
- Frontmatter 照 `src/content.config.ts` 的 schema：
  ```yaml
  title: 依主題和重點下的標題（含讀者會搜尋的關鍵字）
  description: 80–120 字摘要
  category: parenting | working | travel | solo
  pubDate: 一律用實際發布當天日期，格式 YYYY-MM-DD，絕不使用未來日期
  draft: true   # 一律先設 true，見下方「5. 發布流程」
  ```
- **pubDate 一律使用實際發布當天日期，同一天多篇時用時間欄位區分順序，絕不使用未來日期**：首頁「最新文章」和「精選」是照 pubDate 新到舊排序，同一天發多篇時，pubDate 加上時間（例如 `2026-07-14T09:00:00`、`2026-07-14T10:00:00`），依實際發布順序遞增，確保排序正確又不會出現尚未到來的日期。
- 內文語氣：第一人稱，帶點幽默自嘲，內容紮實、有觀察和留白，不要空洞雞湯。可參考 `/about/` 頁面的自我介紹
- **自稱與用詞的規則（重要）**：
  - **不要用「老阿姨」**，一次都不要。作者覺得那個說法有說教味，已經從全站移除
  - 小孩一律稱「小企鵝」（家裡偶爾叫「老闆」，但寫作以小企鵝為主）
  - **「副本」是招牌，但要節制**：一篇最多一次，分類名和標題都不要用。它現在只留在頁尾標語
  - 不要寫「M7 MBA」或解釋商學院排名
  - description 用第一人稱
- **不要寫的東西**：
  - 對稱排比最多一組（「不是 A，是 B」「這不是⋯⋯這是⋯⋯」連續堆疊是最明顯的 AI 味）
  - 結尾不要用「三件套」（總結＋昇華＋呼籲），也不要用漂亮但空的對句收尾
  - 不要假問句（「你是不是也曾經⋯⋯？」）
  - 不要公式化開場（「在這個⋯⋯的時代」）
  - 文末不要放孤立的「⋯⋯」
- **標點**：一律全形（，。：？！），不要出現半形逗號或冒號。台灣用語，不要中國用語（優化→最佳化、視頻→影片）
- **引用的鐵則**：Google AI 摘要、AI 整理的資料一律不可引用。任何數據、研究、名言都要有可查證的原始出處，查不到就不要寫
- 只有使用者給的重點/大綱要擴寫成完整文章，不要無中生有掰不存在的細節（時間地點人物等事實類細節如果沒提供就寫得模糊一點，或留白讓使用者自己補）

## 5. 發布流程
1. `git add` 新文章的 `.md` 和 `public/images/<slug>/` 底下的圖片
2. commit + push 到 `main`（draft: true 不會出現在正式網站上，可以直接 push，不會誤上線）
3. 推上去之後：
   - 把完整文章內容貼在對話裡給使用者看一次
   - 提醒使用者：可以到 [Decap CMS 後台](https://penguinmama.blog/admin/) 找到這篇（會混在文章列表裡），確認沒問題後把「草稿」勾選拿掉、存檔，就會正式上線
4. 不用自己把 draft 改成 false 上線——使用者說她會自己審過再改

## 已知限制
- draft: true 的文章不會產生任何頁面（Astro 靜態建置時會濾掉），所以沒辦法用網址直接預覽，只能看貼在對話裡的內容，或使用者自己跑 `npm run dev` 在本機看
